#include <stdio.h>

/**
 *main - print if the number is postive, zero, or negative
 *
 * Description: print the alphabet in lowercase
 * Return: 0 (success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
